# ── Dependencies ─────────────────────────────────────────────────────────────
node_modules
.pnp
.pnp.*

# ── Turbo ────────────────────────────────────────────────────────────────────
.turbo

# ── Next.js ──────────────────────────────────────────────────────────────────
.next/
out/
build/

# ── Expo / React Native ───────────────────────────────────────────────────────
.expo/
dist/
android/
ios/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# ── Generated ────────────────────────────────────────────────────────────────
app/generated/prisma
apps/web/app/generated/prisma
*.tsbuildinfo
next-env.d.ts
apps/web/next-env.d.ts

# ── Env ──────────────────────────────────────────────────────────────────────
.env
.env.local
.env*.local

# ── Misc ─────────────────────────────────────────────────────────────────────
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
coverage

# ── Claude Code ──────────────────────────────────────────────────────────────
.claude/

# Playwright MCP debug artifacts (any location)
.playwright-mcp/
apps/web/.playwright-mcp/
.gstack/

# Playwright run artifacts (traces, failure screenshots, HTML report)
test-results/
playwright-report/

# Ad-hoc QA / demo screenshots dropped at the repo root
/demo-desktop.png
/qa-*.png

# Local agent/tooling state — never committed
.codex/
.vercel

# `vercel link` appends a bare `.env*` here, which also hides the tracked
# apps/web/.env.example. Lines 35-37 already cover real env files; keep the
# example visible so new contributors can see what needs configuring.
!apps/web/.env.example
